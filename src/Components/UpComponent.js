import React from 'react';
import {connect} from 'react-redux';

const UpComponent = ({}) => {

    return(
        <div>
            InComponent
        </div>
    );
};


export default connect(
    (state) => ({

    }),

    dispatch => ({

    })
)(UpComponent);